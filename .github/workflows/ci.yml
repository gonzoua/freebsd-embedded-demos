name: CI build

on:
  push:
    branches:
      - actions

jobs:
  build:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v2
    - name: build everything
      run: |
          uname -a
          pwd
          ls -la
          find .
          make
          date
